document.addEventListener("DOMContentLoaded", () => {
    const homeBtn = document.getElementById("member-details-list");

    homeBtn.addEventListener("click", () => {
        console.log("Home menu btn clicked");
    });
});